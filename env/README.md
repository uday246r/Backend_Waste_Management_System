# Environment Configuration

1. Duplicate `.env.example` and rename the copy to `.env` inside this folder.
2. Fill in all placeholder values with the secrets for your deployment.
3. Keep `.env` private—it is ignored by git via `.gitignore`.

